# ルール表示用データ

- `quick-reference.js`: 盤面下部のルール枠で使う早見表データです。別画面でルールを表示する場合も、このモジュールを import して再利用できます。
- `images/`: ルール説明専用の画像を置く想定のディレクトリです。`quick-reference.js` では `rules/images/example.webp` のように参照できます。
